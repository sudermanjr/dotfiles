export GOPATH=~/go

gotest() {
    printf "Linter:\n"
    go list ./... | xargs -L1 golint
    printf "\n\nTests:\n\n"
    go test -v --bench --benchmem -coverprofile cover.out ./...

    go tool cover -html=cover.out -o cover.html
    printf "\nCoverage report available at cover.html\n\n"
}
