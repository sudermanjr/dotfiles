# enable color support of ls and also add handy aliases

export LS_OPTIONS='--color=auto'
eval "`dircolors`"
alias ls='ls $LS_OPTIONS'

LS_COLORS=$LS_COLORS:'di=0;36:fi=0;37:ex=0;92:' ; export LS_COLORS

# some more ls aliases
#alias ll='ls -l'
#alias la='ls -A'
#alias l='ls -CF'
